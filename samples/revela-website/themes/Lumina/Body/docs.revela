{{~##
Docs Body Template - Documentation with Sidebar

Renders a documentation page with sidebar navigation.
Supports nested documentation (e.g., docs/source-structure/static-files).

Expects:
- gallery: { title, body }
- nav_items: Array of NavigationItem for sidebar
##~}}
<aside>
    <nav>
        <ul>
{{~ for item in nav_items ~}}
{{~ if item.active || item.text == gallery.title ~}}
{{~ for child in item.children }}
{{~ child_link = page_url(child) ~}}
            <li>
{{~ if child_link ~}}
                <a href="{{ child_link }}"{{ if child.current }} class="active"{{ end }}>{{ child.text }}</a>
{{~ else ~}}
                <span>{{ child.text }}</span>
{{~ end ~}}
{{~ if child.children ~}}
                <ul>
{{~ for grandchild in child.children ~}}
{{~ grandchild_link = page_url(grandchild) ~}}
{{~ if grandchild_link ~}}
                    <li><a href="{{ grandchild_link }}"{{ if grandchild.current }} class="active"{{ end }}>{{ grandchild.text }}</a></li>
{{~ else ~}}
                    <li><span>{{ grandchild.text }}</span></li>
{{~ end ~}}
{{~ end ~}}
                </ul>
{{~ end ~}}
            </li>
{{~ end ~}}
{{~ end ~}}
{{~ end ~}}
        </ul>
    </nav>
</aside>
<article>
{{~ if gallery.title ~}}
    <h1>{{ gallery.title }}</h1>
{{~ end ~}}
{{~ if gallery.body ~}}
{{ gallery.body }}
{{~ end ~}}
</article>
