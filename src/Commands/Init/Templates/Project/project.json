{
  "name": "{{ project.name }}",
  "url": "{{ project.url }}",
  "theme": "{{ project.theme }}",
  "generate": {
    "images": {
      "formats": {
        "jpg": 90
      },
      "sizes": [640, 1280, 1920]
    }
  }
}
